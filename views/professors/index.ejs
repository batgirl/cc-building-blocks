<!DOCTYPE html>
<html>
  <head>
    <title>Building Blocks</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.5/cosmo/bootstrap.min.css">
    <link rel="stylesheet" href="stylesheets/style.css">
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">Building Blocks</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if (user) { %>
            <li><a href="/signout">Sign Out</a></li>
            <% } else { %>
            <li><a href="/signup">Sign Up</a></li>
            <li><a href="/login">Log In</a></li>
            <% }%>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">
      <h2>All courses:</h2>
      <% if (courses.length) { %>
      <table class="table table-bordered table-hover">
        <tr>
          <th>Title</th>
          <th>Department</th>
          <th>Block</th>
          <% if (!user.courseId) { %><th></th><% } %>
        </tr>
        <% for (var i = 0; i < courses.length; i++) { %>
        <tr>
          <td><%= courses[i].title %></td>
          <td><%= courses[i].department %></td>
          <td><%= courses[i].block %></td>
          <% if (!user.courseId) { %>
          <td>
            <form action="/professors/courses" method="post"><button type="submit" class="btn btn-success btn-xs" name="courseId" value="<%= courses[i]._id %>">Select this Course</button></form>
          </td>
          <% } %>
        </tr>
        <% } %>
      </table>
      <% } else { %>
      <p class="lead">There are no courses to show.</p>
      <% } %>
      <% if (user.courseId) { %>
      <h2>Your course:</h2>
      <p class="lead"><strong><%= professorCourse.title %></strong><br>
        <em><%= professorCourse.department %>, Block <%= professorCourse.block %></em>
      <hr>
      <form action="/professors/courses/removeCourse" method="post"><button type="submit" class="btn btn-warning btn-xs" name="courseId" value="<%= professorCourse._id %>">Remove this Course</button></form></p>
      <% } %>
    </div>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  </body>
</html>
